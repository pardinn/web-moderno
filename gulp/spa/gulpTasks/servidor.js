const gulp = require('gulp')

function monitorarArquivos(callback) {
    return callback()
}

function servidor(callback) {
    return callback()
}

module.exports = {
    monitorarArquivos,
    servidor
}